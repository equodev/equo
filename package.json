{
  "name": "framework",
  "description": "[Equo Framework](https://www.equoplatform.com/framework) is an open source SDK which allows users to write modern GUI applications once, and deploy them across desktop and embedded operating systems without rewriting the source code.",
  "scripts": {
    "lint": "eslint . --ext .ts,.js,.json --fix",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.6",
    "eslint-plugin-json-format": "^2.0.1",
    "husky": "^7.0.1",
    "standard": "^16.0.3",
    "ts-standard": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
