image: registry.gitlab.com/maketechnology/equo/docker/gradle:jdk8-alpine

stages:
  - build

export:
  stage: build
  script:
    - gradle check
    - gradle release
    - gradle gitPublishPush
  artifacts:
    paths:
      - cnf/equoapp.bnd
      - cnf/release
    expire_in: 2m

